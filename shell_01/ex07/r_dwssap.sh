cat /etc/passwd | grep -v '#' | cut -d ':' -f 1 | awk 'NR % 2 == 0' | awk -F ':' '{ print $1 }' | rev | sort -r | awk ''"$FT_LINE1"' <= NR && '"$FT_LINE2"' >= NR' | tr '\n' ',' | sed 's/,/, /g' | sed 's/, $/./g' | tr -d '\n'
